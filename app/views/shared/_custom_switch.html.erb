<div>
  <% if current_user.selections.where(appartement_id: appartement.id) == []%>
    <%= link_to "", appartement_selections_path(appartement), method: :post, remote: true, class: "far fa-star"%>
    <% else %>
    <%= link_to "", selection_path(current_user.selections.where(appartement_id: appartement.id)[0]), remote: true, method: :delete, class: "fas fa-star"  %>
    <% end %>
  </div>
